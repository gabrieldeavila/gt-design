import { darkCodeTheme, lightCodeTheme } from "../../utils/defaultsTheme";

const defaultConfigs = {
  theme: "gt-design",
  themeConfig: {
    global: {
      theme: lightCodeTheme,
      darkTheme: darkCodeTheme,
    },
  },
};

export default defaultConfigs;
